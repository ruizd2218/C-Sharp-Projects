using System;
class SampleApplication {
    // Main() is a function that tells the compiler to execute anything between 
    // the curly braces. In a console application, a function with the name 
    // “Main()” is automatically executed as the first step in running the program
    static void Main() {
        Console.WriteLine("Hello, World");
    }
}